services:
  mcp-lk-print-box:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-lk-print-box
    restart: unless-stopped
    # 端口映射（如果需要HTTP接口）
    ports:
      - "${MCP_PORT:-8000}:8000"
